<h1>All Users</h1>

<% @users.each do |user| %>
  <article id='user-<%= user.id %>'>
    <p><%= link_to user.name, "/admin/users/#{user.id}" %></p>
    <p><%= "Date Registered: #{user.created_at}" %></p>
    <p><%= "Role: #{user.role}" %></p>
  </article>
<% end %>
